#!/bin/bash
n=0; x=0;
for ((c = 1; ; c++)) do
  if (! find $c.in); then break; fi
  let "x = x + 1";
  echo $c
  timeout 2s ./rotate < $c.in > rotate.out
  if diff -w $c.out rotate.out > /dev/null ; then let "n = n + 1"; 
    else diff -w $c.out rotate.out; fi
done
echo "Correct tests: $n/$x"

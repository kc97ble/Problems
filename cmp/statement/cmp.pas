unit cmp;

interface

{ these are just prototypes that you must implement }
procedure remember(n:integer);
function compare(b:integer):integer; 

implementation

uses cmpdata;

procedure remember(n:integer);
begin 
  {set_bit(n); //example }
end;

function compare(b:integer):integer;
begin
  {get_bit(n); //example }
end;


end.
